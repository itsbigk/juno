describe('menu service testing', function() {
  var Menu, httpBackend, menu;

  beforeEach(inject(function(_Menu_, $httpBackend) {
    Menu = _Menu_;
    httpBackend = $httpBackend
  }));

  it('should get a menu from a specific restaurant', function() {
    
  })
});
